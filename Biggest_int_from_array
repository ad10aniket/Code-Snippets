public static int[] biggestByLength(int[][] arrays) {
    if (arrays == null || arrays.length == 0) return null;

    int[] biggest = arrays[0];

    for (int[] arr : arrays) {
        if (arr != null && arr.length > biggest.length) {
            biggest = arr;
        }
    }
    return biggest;
}
